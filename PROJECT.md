# TriDi — сводка по проекту

Единый обзор сервиса: что за продукт, из каких частей он состоит, какие функции и блоки уже
реализованы, где что лежит и что ещё предстоит сделать. Документ описывает состояние
репозитория на момент последнего коммита.

- **Продукт:** онлайн-курсы по 3D-печати. Базовый курс — бесплатно, продвинутый курс и
  конструктор 3D-моделей — по подписке. Внутри: уроки с блоками, тесты модулей с пояснениями,
  личный кабинет с прогрессом, юридический раздел по 152-ФЗ.
- **Домен:** https://tridi-print.ru
- **Владелец (он же оператор персональных данных):** самозанятый Накоряков Вадим Сергеевич,
  ИНН 531603515195 (НПД). Контакты для обращений — nvs16@ya.ru.
- **Стек:** Next.js 15.5.25 (App Router, Turbopack), React 19, TypeScript 5, Tailwind CSS 4,
  Supabase (@supabase/ssr 0.12.7 + supabase-js 2.116, self-hosted на VPS в РФ),
  three.js / @react-three (в зависимостях), хостинг приложения — Amvera Cloud (Москва).
- **Архитектурный принцип:** данные курсов и юридические тексты лежат отдельно от отображения;
  страницы — серверные компоненты, клиентского Supabase в браузере нет (авторизация идёт
  через Server Actions), доступ к платному контенту решается только на сервере.

---

## 1. Быстрый старт

Переменные окружения (файл `.env.local`, шаблон — `.env.example`):

| Переменная | Назначение |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | URL проекта Supabase |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Публичный ключ (anon) |
| `SUPABASE_SERVICE_ROLE_KEY` | Приватный ключ; в коде пока не используется (задел для серверных задач) |
| `NEXT_PUBLIC_SITE_URL` | Публичный адрес сайта для ссылок в письмах (подтверждение email) |

Команды (`package.json`):

| Команда | Что делает |
|---|---|
| `npm run dev` | Дев-сервер (`next dev --turbopack`) |
| `npm run build` | `ensure:tsconfig` + продакшн-сборка |
| `npm run build:amvera` | То же для хостинга: `clean` + `ensure:tsconfig` + `check:imports` + `npm ci --include=optional` + сборка |
| `npm run start` | Запуск собранного приложения |
| `npm run lint` | ESLint (`eslint-config-next`) |
| `npm run check:docs` | Проверка сводки `PROJECT.md` вместе с данными: все упомянутые пути существуют, числа уроков/блоков/вопросов совпадают, заявленные `total_*` не разошлись с содержимым |
| `npm run check:imports` | Проверка, что все `@/…` и относительные импорты существуют **и с тем же регистром**, что на диске (Linux на Amvera регистр различает, Windows — нет) |
| `npm run ensure:tsconfig` | Восстанавливает `tsconfig.json` с алиасом `@/*`, если файл не доехал до сборочного дерева |
| `npm run clean` | Удаляет `.next` |

---

## 2. Маршруты

Публичные (без входа):

| URL | Файл | Что показывает |
|---|---|---|
| `/` | `app/page.tsx` | Витрина: обещание, выгоды, программа из двух курсов, тарифы, «О проекте». `force-dynamic` из-за кнопки авторизации в шапке |
| `/subscription` | `app/subscription/page.tsx` | Тарифы Free / Maker / Pro, состав каждого, FAQ, примечание про оферту и чек НПД |
| `/legal` | `app/legal/page.tsx` | Оглавление юридических документов |
| `/legal/privacy`, `/legal/consent`, `/legal/terms`, `/legal/offer`, `/legal/cookies` | `app/legal/*/page.tsx` | Пять документов (статически пререндерятся, открываются даже когда база недоступна) |

Авторизация и верификация:

| URL | Файл | Что делает |
|---|---|---|
| `/auth/login` | `app/auth/login/page.tsx` + `LoginForm.tsx` | Вход по email и паролю, возврат по `next`, ссылка «отправить письмо ещё раз» |
| `/auth/sign-up` | `app/auth/sign-up/page.tsx` + `SignUpForm.tsx` | Регистрация, обязательный чекбокс согласия, пароль ≥ 6 символов |
| `/auth/verify-email` | `app/auth/verify-email/page.tsx` + `ResendForm.tsx` | «Проверьте почту» / «завершаем подтверждение» / «ссылка не сработала» / «почта подтверждена» + повторная отправка письма |
| `/auth/confirm` | `app/auth/confirm/route.ts` | **Route Handler**: принимает ссылку из письма (`?token_hash=&type=` или `?code=`), проверяет токен и записывает cookies сессии |

Закрытые (нужен вход; для платного контента — ещё и подписка):

| URL | Файл | Что показывает |
|---|---|---|
| `/dashboard` | `app/dashboard/page.tsx` | Личный кабинет: имя, прогресс базового курса «X из 15», процент, выход |
| `/course/lesson-1…15` | `app/course/[id]/page.tsx` | Урок базового курса: блоки, прогресс, кнопка «Пройти урок»; на последнем уроке модуля — карточка «Проверь себя» со слайдом теста и заблокированным продолжением |
| `/course/check/1…6` | `app/course/check/[moduleId]/page.tsx` | Тест модуля базового курса (10 вопросов с пояснениями); тот же тест открывается слайдом прямо с урока |
| `/course/pro/<модуль>/<урок>` | `app/course/pro/[moduleId]/[lessonId]/page.tsx` | Урок продвинутого курса: превью 2 блоков без подписки, замок, гейт по тестам модулей |
| `/course/pro/<модуль>/check` | `app/course/pro/[moduleId]/check/page.tsx` | Тест модуля продвинутого курса (проверка подписки) |
| `/constructor` | `app/constructor/page.tsx` | Конструктор 3D-моделей — пока **заглушка**: «Раздел в разработке» и три карточки шаблонов (Вазы, Брелоки, Светильники) со статусом «Скоро»; `robots: noindex` |

Middleware (`middleware.ts` → `lib/supabase/middleware.ts`) работает только на `/dashboard/*`,
`/course/*`, `/constructor/*` и `/auth/*`: продлевает сессию, отправляет гостя на вход с
запоминанием адреса (`next=`), не пускает в курсы, конструктор и кабинет пользователя с
**неподтверждённой почтой** (`email_confirmed_at`), а авторизованного уводит со страниц входа.
Список закрытых префиксов лежит в `PROTECTED_PATH_PREFIXES` (`lib/supabase/middleware.ts`),
а сам matcher — в `middleware.ts`. Публичные страницы (главная, тарифы, `/legal`) middleware не
трогает — им сессия не нужна.

---

## 3. Функции по блокам

### 3.1 Регистрация и подтверждение email

Полный цикл: **регистрация → письмо → переход по ссылке → сессия → доступ**.

1. `signUpAction` (`app/actions/auth.ts`) создаёт аккаунт серверным клиентом Supabase и передаёт
   `emailRedirectTo` — адрес `/auth/confirm` на нашем домене плюс `next`, куда вернуть человека.
   Адрес строит `lib/site-url.ts`: `NEXT_PUBLIC_SITE_URL` → (в разработке) хост из заголовка
   запроса → боевой домен `https://tridi-print.ru`. Заголовок `Host` учитывается только вне
   продакшена, чтобы адрес в письме нельзя было подменить.
2. Если Supabase вернул сессию (подтверждение в проекте выключено) — сразу вход; если нет —
   редирект на `/auth/verify-email?next=…` с сообщением «мы отправили письмо».
3. Ссылку из письма обрабатывает Route Handler `app/auth/confirm/route.ts`. Поддерживаются
   **три формата**, потому что шаблоны писем у Supabase бывают разные:

   | Формат ссылки | Как обрабатывается |
   |---|---|
   | `?token_hash=…&type=…` (рекомендуемый) | `supabase.auth.verifyOtp()`, работает без JavaScript |
   | `?code=…` (PKCE) | `supabase.auth.exchangeCodeForSession()` |
   | `#access_token=…` (шаблон Supabase по умолчанию) | фрагмент адреса не уходит на сервер, поэтому его разбирает клиентский `components/EmailLinkHandler.tsx` и передаёт токены в Server Action `app/actions/confirm-session.ts` (`setSession`) |

   `type` сверяется со списком разрешённых, `next` санитизируется (`resolveAfterAuthPath`, только
   внутренние пути), после чего браузер идёт на `/auth/verify-email?status=ok|error|checking&next=…`.
4. Это Route Handler, а не страница, по существу: в Server Component запись cookies запрещена,
   а `lib/supabase/server.ts` ошибки записи глушит — страница молча не сохранила бы сессию.
5. `/auth/verify-email` не только показывает статус: там форма повторной отправки
   (`resendVerificationAction`, письмо уходит с тем же `emailRedirectTo`). Ответ нейтральный
   («если адрес зарегистрирован и не подтверждён — письмо отправлено»), чтобы форма не стала
   инструментом проверки чужих адресов; лимит отправок обрабатывается отдельным сообщением.
6. Пока почта не подтверждена, middleware не пускает в `/course/*` и `/dashboard` — человек
   попадает на `/auth/verify-email?next=<куда шёл>`. Страница подтверждения и `/auth/confirm`
   открыты и вошедшим: иначе получилась бы петля (middleware → кабинет → middleware), а токен
   из письма остался бы неиспользованным.

**Настройка на стороне владельца (self-hosted Supabase):** Site URL и Redirect URLs
(`https://tridi-print.ru/**`), шаблон письма **Confirm signup** со ссылкой
`{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=signup`; на хостинге —
`NEXT_PUBLIC_SITE_URL`. Подробнее — README, раздел «Подтверждение email».

### 3.2 Вход, выход и восстановление доступа

- Вход — `signInAction`: email и пароль, ошибки Supabase переводятся на русский
  (`translateSignInError`): «Email ещё не подтверждён…» и лимит попыток подсказывают, что делать;
  всё остальное сводится к «Неверный email или пароль», чтобы не подсказывать, существует ли
  аккаунт. При успехе сервер сам записывает cookies сессии и уводит на `next` (или в кабинет).
- Выход — форма прямо в кабинете (`app/dashboard/page.tsx`): серверная функция вызывает
  `supabase.auth.signOut()` и делает `redirect("/")`. Отдельного action-файла для выхода нет.
- Вошедшему, но не подтвердившему почту пользователю закрытые разделы недоступны, а страница
  подтверждения предлагает отправить письмо снова.
- **Сброс пароля пока не реализован** — см. раздел 12 «Ограничения».

### 3.3 Доступ и подписка

Доступ решается в четырёх слоях, чтобы платный контент нельзя было получить в обход:

1. **Аккаунт** — есть ли вход (`lib/current-user.ts`).
2. **Подписка** — `subscriptions.status`: `active`, либо `trial` с `trial_ends_at` в будущем.
   Эту проверку делают страницы продвинутого курса (`canAccessProCourse` в `lib/basic-course.ts`
   и одноимённая локальная функция на странице урока/теста).
3. **Закрытый контент** — без подписки отдаются только первые **2 блока** урока
   (`PREVIEW_BLOCKS` в `app/course/pro/[moduleId]/[lessonId]/page.tsx`), дальше — замок со ссылкой
   на тарифы. Названия уроков и программа видны полностью: это осознанное решение — человек
   понимает, что покупает, но контента не получает.
4. **Прогрессия** — следующий модуль продвинутого курса открывается только после сданного теста
   предыдущего (`lib/module-test-progress.ts`).
5. **База** — `supabase/pro_course_access.sql` и `supabase/grants_and_policies.sql` (RLS-политики
   и гранты): анонимный доступ закрыт, строки прогресса видны только владельцу.

Тарифы: Free — 0 ₽ (базовый курс, тесты модулей, конструктор в ограниченном режиме),
Maker — 490 ₽/мес (бесконечные скачивания моделей, коммерческая лицензия, AI-диагностика),
Pro — 1 490 ₽/мес (Maker + продвинутый курс и его тесты, пакетная генерация, API, ранний доступ).
Состав тарифов и FAQ — в `app/subscription/page.tsx`; цена Pro берётся из `data/courses.json`
(`pro.price`) — того же файла, что использует страница урока продвинутого курса, чтобы цена в
замке и на витрине не разошлась. Цены продублированы в публичной оферте (`app/legal/offer`).
Кнопка оплаты (`app/subscription/PlanButton.tsx`) пока показывает «оплата подключается»:
платёжный провайдер не подключён, а порядок оплаты, чеки НПД и возврат уже описаны в оферте.
Функции «конструктор» и «AI-диагностика» из тарифов ещё не реализованы — см. раздел 12.

### 3.4 Базовый курс

- 6 модулей, 15 уроков, 150 блоков — ровно по 10 блоков в каждом уроке.
- Программа: `data/lessons.json` (`modules[].module_title`, `lessons[].lesson_title`).
- Страница урока `app/course/[id]/page.tsx` (адреса `/course/lesson-1…15`): блоки через
  `components/course/LessonBlocks.tsx`, прогресс курса, кнопка «Пройти урок» (`completeLesson`,
  уроки отмечаются только по порядку — «перескок» невозможен), переход к следующему уроку.
- На последнем уроке модуля появляется карточка «Проверь себя»: тест открывается **слайдом**
  поверх урока (`components/course/TestOverlay.tsx`), а кнопка продолжения заблокирована до сдачи
  («Сначала сдай тест»). Тот же тест доступен отдельной страницей `/course/check/<код>`.
  Экран «Модуль закрыт» у следующего модуля устроен так же: тест можно пройти на месте.
- Разворачивание модульной структуры в плоский список и поиск модуля — `lib/basic-course.ts`
  (номера уроков там же совпадают с `lesson_id`, поэтому старые записи прогресса не потребовали
  миграций).

### 3.5 Продвинутый курс

- 26 модулей, 109 уроков, 741 блок. Темы: диагностика дефектов, материалы, размеры и допуски,
  прочность, постобработка, серийное производство, заработок на печати.
- Программа: `data/course-pro.json`; страницы — `app/course/pro/**`
  (`[moduleId]/[lessonId]/page.tsx` и `[moduleId]/check/page.tsx`).
- Прогресс уроков пишется тем же таблицам, что и базовый курс, но с `course_type = "pro"`,
  результаты тестов — с `course_type = "pro-test"` (у базового — `basic` и `basic-test`).
  Разделение типов нужно, чтобы строки тестов не искажали счётчик уроков «X из 109».
- Номер модуля в базе для продвинутого курса — не буквенный код («A», «T-тизер»), а порядковый
  номер (`module_order`, 1…26): это делает `isModuleTestCompletedByOrder`.

### 3.6 Тесты модулей

- 10 вопросов на модуль, 4 варианта ответа, **3 пояснения** к каждому вопросу — к правильному
  ответу и к каждому неправильному, чтобы разбор был осмысленным.
- Пороги: сдано с **5** правильных, **8 и больше** — «отлично» (`MODULE_TEST_PASS_FROM`,
  `MODULE_TEST_EXCELLENT_FROM` в `lib/module-test.ts`). Попытки не ограничены.
- **Сохраняется только сданная попытка** (`app/actions/complete-module-test.ts`): наличие строки
  означает «модуль сдан», по ней открывается следующий модуль. Провал не пишется — иначе модуль
  открылся бы сам.
- Компонент `components/course/ModuleTest.tsx` — клиентский: по одному вопросу, сразу пояснение,
  в финале — оценка и разбор; после сдачи результат уходит на сервер и страница обновляется.
- Тест открывается **слайдом** поверх урока (`components/course/TestOverlay.tsx`): затемнение,
  `role="dialog"` + `aria-modal`, закрытие по Escape и клику по фону, возврат фокуса на кнопку.
  Пока слайд открыт, страница под ним не прокручивается; после сдачи вызывается `router.refresh()`,
  и заблокированная кнопка продолжения разблокируется без ручной перезагрузки.
- Карточку на уроке рисует `components/course/ModuleTestGate.tsx`: при несданном тесте — кнопка
  «Пройти тест» и **заблокированное** продолжение с подписью «Сначала сдай тест» (дальше не пускаем),
  при сданном — активное «Продолжить курс» и «Пройти тест ещё раз». Цвета курса — в
  `components/course/module-test-theme.ts` (общий модуль без `"use client"`, поэтому его читают и
  серверные, и клиентские компоненты).
- `ModuleTest` работает в двух режимах: `layout="page"` (страница `/course/check/<код>`) и
  `layout="overlay"` (слайд; внутри — кнопка «Закрыть» вместо ссылки «к урокам модуля»).
- Если результаты прочитать не удалось (нет сети или таблицы), считается, что тест сдан:
  запереть ученика из-за сбоя чтения хуже, чем показать урок.

### 3.7 Личный кабинет

`app/dashboard/page.tsx`: приветствие по имени (или email), прогресс базового курса
(«X из 15», процент, полоса), кнопка «Продолжить обучение» на следующий непройденный урок,
итоги тестов, ссылка на юридические документы, выход. Счётчики для кабинета и витрины считает
`lib/course-stats.ts`.

### 3.8 Юридический контур

- Пять документов в `app/legal/*`: **Политика обработки персональных данных** (28 КБ — основной
  документ по 152-ФЗ), **Согласие на обработку персональных данных**, **Пользовательское
  соглашение**, **Публичная оферта**, **Политика использования cookie** + страница-оглавление
  `/legal`.
- Реквизиты владельца (самозанятый Накоряков Вадим Сергеевич, ИНН 531603515195, НПД, домен,
  почта) и дата редакции (`LEGAL_UPDATED_AT`) лежат в одном месте — `lib/legal.ts`; оттуда их
  берут документы, футер и раздел в кабинете.
- Общий каркас `/legal` — `app/legal/layout.tsx`: своя упрощённая шапка (без чтения сессии),
  навигация по документам и реквизиты в подвале. Раздел пререндерится статически и открывается,
  даже если база недоступна.
- Тексты документов свёрстаны через `components/legal/LegalText.tsx` (заголовки, списки,
  выделенные блоки, внутренние ссылки) — чтобы пять больших документов выглядели одинаково.
- Cookie-баннер `components/CookieBanner.tsx`: два равноправных варианта («Только необходимые» /
  «Принять все»), выбор с датой сохраняется в `localStorage` (`tridi-cookie-consent`), повторно
  не показывается. Аналитики на сайте нет — когда появится, её можно включать только при
  согласии `scope === "all"`; для этого есть `readCookieConsent()`.
- Согласие на обработку — обязательный чекбокс в форме регистрации со ссылками на политику и
  согласие; без него аккаунт не создаётся.
- В документах описаны права субъекта (доступ, уточнение, удаление, отзыв согласия, жалоба в
  Роскомнадзор), сроки хранения, состав данных, порядок оплаты и возврата, чек НПД через
  приложение «Мой налог».

### 3.9 Витрина и тарифы

- `app/page.tsx` — главная: обещание и целевая аудитория, выгоды, программа двух курсов
  (числа берутся из данных, а не вписаны руками), тарифы, блок «О проекте», призыв к регистрации.
  Страница `force-dynamic`: шапка показывает состояние входа, значит читает cookies.
- `app/subscription/page.tsx` — подробное сравнение тарифов Free / Maker / Pro, состав каждого,
  что входит, как оплатить, FAQ (можно ли вернуть деньги, нужен ли опыт, как быстро открывается
  доступ). Ссылка сюда приходит из замка в уроках продвинутого курса (`?locked=true`).
- `components/Header.tsx` — шапка с учётом сессии (вход / кабинет), `components/Footer.tsx` —
  подвал со всеми юридическими ссылками и реквизитами; оба подключены на всех страницах.

### 3.10 Визуализации уроков (Этап 2: идёт)

Механика готова, базовый курс рисуется. Реестр `components/lesson-visuals/index.tsx` сопоставляет
блоку `image/animation/screenshot/diagram` отдельный компонент по ключу «курс + модуль + урок + тип +
номер блока» (`basic-1-image-0`, `pro-A-A1-image-0`), каждая визуализация — отдельный файл
(`components/lesson-visuals/basic-1-image-0.tsx`), общая обёртка — `_Wrapper.tsx`.

Готово: базовый курс заполнен целиком — **27 из 27 визуальных блоков** (M1–M6, уроки 1–15):
модуль 1 «Знакомство» (1–3), модуль 2 «Настройка печати» (4–7), модуль 3 «Практика» (8–9), модуль 4
«Проблемы и решения» (10–11), модуль 5 «Быстрые победы» (12–13), модуль 6 «Финал» (14–15). Продвинутый
раздел: 205 блоков с `visual_file` ждут своих компонентов. До этого каждый такой блок рисуется
заглушкой с иконкой своего типа: лучше заглушка, чем чужая картинка в уроке. Добавление визуализации =
новый файл плюс одна строка в реестре.

Правила визуализаций (проверяются перед коммитом каждого модуля):

- один блок — один компонент — один файл, повторов между уроками нет;
- кадр всегда `viewBox="0 0 320 180"`, подписи не выходят за него — замер живой вёрстки, а не на глаз;
- минимум подписи — 10 px по кадру: на телефоне кадр сжимается до 294 px, то есть ≈9,2 px на экране;
- **исключение из минимума 10 px — сетка 2×5 в `basic-10-image-0`**: плитки 52 px, шрифт 8 px
  (≈7,4 px на телефоне). Это превью-миниатюры десяти дефектов, и тот же список целиком есть в тексте
  урока: подписи дублируются, а сетка даёт обзор всего брака сразу. Отдельно буква-логотип 9,5 px в
  `basic-11-image-1` — она дублируется названием площадки рядом.

---

### 3.11 Конструктор моделей (заглушка)

Ссылки «Перейти в конструктор» стоят на финале базового курса, в уроке и тесте продвинутого
курса и в кабинете после завершения обучения — раньше они вели в 404. Теперь `/constructor`
открывается и честно сообщает, что раздел в разработке: три карточки — **Вазы**, **Брелоки**,
**Светильники** — со статусом «Скоро» и списком того, что в каждом шаблоне можно будет
настраивать, плюс блок «Пока раздел готовится» со ссылками в кабинет и на тарифы.

Страница под авторизацией (путь есть и в `matcher` middleware, и в `PROTECTED_PATH_PREFIXES`) и
с `robots: noindex`. Когда появится рабочий конструктор, готовую страницу заменяет настоящая —
структура карточек под неё уже подготовлена.

---

## 4. Блоки урока

Все блоки рисует `components/course/LessonBlocks.tsx`; незнакомый тип блока не ломает страницу —
он просто не отображается. Форма блока описана в `lib/types.ts` (`LessonBlock`).

| Тип блока | Что показывает | Поля данных |
|---|---|---|
| `text` | Абзац с заголовком | `title`, `content` |
| `list` | Маркированный список | `title?`, `items[]` |
| `steps` | Инструкция по шагам (нумерация, «шаг N») | `title?`, `steps[]` (строка или «заголовок + пояснение») |
| `tip` | «Совет» — акцентная врезка | `title`, `content` |
| `warning` | «Важно» — предупреждение | `title`, `content` |
| `analogy` | Аналогия — образное объяснение | `title`, `content` |
| `image` | Иллюстрация; пока заглушка с иконкой | `title`, `content` (описание), `visual_file`, `src?` |
| `animation` | Анимация/видео; пока заглушка | `title`, `content`, `visual_file` |
| `screenshot` | Скриншот интерфейса; пока заглушка | `title`, `content`, `visual_file` |
| `diagram` | Схема; пока заглушка | `title`, `content`, `visual_file` |
| `mini_check` | Мини-проверка в уроке (`components/course/MiniCheck.tsx`) | `questions[]` — вопрос, 4+ варианта, номер правильного, пояснения |

`mini_check` — **инфраструктура «на будущее»**: в текущих данных обоих курсов такие блоки не
используются, проверка знаний живёт на страницах тестов модулей. Компонент оставлен рабочим,
если понадобится короткая проверка прямо в уроке.

---

## 5. Данные

Все курсы — это JSON в репозитории; база используется только для пользователей, доступа и
прогресса. Никакой CMS нет: чтобы поправить урок, правится файл данных.

| Файл | Размер | Что внутри | Кто использует |
|---|---|---|---|
| `data/lessons.json` | 102 КБ | Базовый курс: 6 модулей, 15 уроков, 150 блоков, 6 тестов модулей (60 вопросов, 180 пояснений) | `lib/basic-course.ts`, главная, тарифы, `/course/lesson-N`, `/course/check/N` |
| `data/course-pro.json` | 380 КБ | Продвинутый курс: 26 модулей, 109 уроков, 741 блок, 26 тестов (260 вопросов, 780 пояснений) | `/course/pro/**`, витрина, кабинет |
| `data/courses.json` | 1 КБ | Витринные метаданные курсов: `title`, `subtitle`, `goal`, `tier`, `totalLessons`, `pro.price` (1 490) | Главная, тарифы, замок в уроке продвинутого курса |

Форма данных — `lib/types.ts`: `Course → modules[] → lessons[]` (`lesson_id`, `lesson_order`,
`lesson_title`, `duration`, `blocks[]`) плюс `module_test` у модуля. Тест модуля — та же форма, что
у блока `mini_check`: `question`, `answers[]`, `correct`, `explanations[]`.

Правила, на которых держатся страницы:

- `lesson_order` — **сквозной** номер урока в курсе (1…15 и 1…109), а не внутри модуля; место урока
  внутри модуля считается по порядку в массиве `lessons`.
- Код модуля у базового курса числовой (`"1"`…`"6"`), у продвинутого — буквенный (`"A"`, `"C1"`,
  `"T-тизер"`). Поэтому в базе для тестов продвинутого курса хранится `module_order` (1…26).
- Числа для витрины считаются по данным (`summarizeCourse`), а не берутся из полей `total_*`:
  поля легко отстают от содержимого, поэтому их совпадение с фактическими числами проверяет
  `npm run check:docs` (раньше `total_lessons` заявлял 110 уроков при 109 — проверка появилась
  после этой ошибки).
- 232 блока ссылаются на будущий файл визуализации (`visual_file`) — по нему на Этапе 2
  собираются SVG/анимации (см. раздел 3.10).
- `data/*.json` импортируются напрямую (`import lessonsData from "@/data/lessons.json"`), поэтому
  содержимое попадает в бандл страницы: файлы читает только серверный код.

---

## 6. Библиотеки `lib/`

| Файл | Назначение |
|---|---|
| `lib/types.ts` | Типы данных курсов: блоки, уроки, модули, курсы, вопросы тестов |
| `lib/basic-course.ts` | Разворачивает базовый курс из модулей в плоский список уроков (`basicLessons`), собирает `basicCourseModules`, ищет модуль по коду (`findBasicModule`); проверяет доступ к продвинутому курсу (`canAccessProCourse`) |
| `lib/course-stats.ts` | Числа и слова для витрины: `summarizeCourse`, `formatHours`, `groupByModule`, `pluralize` и формы слов («1 урок / 2 урока / 5 уроков») |
| `lib/module-test.ts` | Правила тестов: пороги сдачи (5) и «отлично» (8), типы записей (`basic-test`, `pro-test`), проверки `isModuleTestPassed`, `isModuleTestCompleted`, `isModuleTestCompletedByOrder` |
| `lib/module-test-progress.ts` | Загружает из базы номера сданных модулей (`Set<number>`); `null` — если прочитать не удалось (тогда модуль не запираем) |
| `lib/current-user.ts` | Кто открыл страницу: берёт пользователя из заголовка middleware, при его отсутствии сам вызывает `getUser()` |
| `lib/auth-redirect.ts` | `authHref(path, next)` — ссылка на вход с возвратом; `resolveAfterAuthPath` / `safeNextPath` — санитизация `next` (только внутренние пути, отсекаем `//evil.com`) |
| `lib/legal.ts` | Реквизиты владельца (`LEGAL_OPERATOR`), дата редакции документов, список юридических документов |
| `lib/site-url.ts` | Публичный адрес сайта и `buildEmailRedirectUrl(next)` для ссылок в письмах: `NEXT_PUBLIC_SITE_URL` → (в dev) хост запроса → боевой домен |
| `lib/supabase/server.ts` | Серверный клиент Supabase с cookies (Server Components и Server Actions) |
| `lib/supabase/client.ts` | Браузерный клиент — **сейчас не используется** (вход идёт через Server Actions): оставлен заготовкой |
| `lib/supabase/middleware.ts` | `updateSession`: обновление сессии, защита закрытых маршрутов, гейт неподтверждённой почты, проброс пользователя заголовком, редиректы авторизованных со страниц входа |
| `lib/supabase/user-headers.ts` | Кодирование и чтение заголовка `x-supabase-user`: страницы не делают лишний сетевой `getUser()`. Заголовок всегда перезаписывается middleware, поэтому подделать его нельзя |
| `middleware.ts` (корень) | Точка входа Next.js с `matcher`: только `/dashboard/:path*`, `/course/:path*`, `/auth/:path*` — публичные страницы middleware не трогает |

---

## 7. Server Actions (серверные функции)

Все лежат в `app/actions/*` и помечены `"use server"`: браузер отправляет форму или вызов, а
выполняется код на сервере — там, где есть cookies и ключи Supabase. Наружу возвращаются только
тексты для пользователя: английские ошибки Supabase переводятся на русский.

| Функция | Файл | Что делает |
|---|---|---|
| `signInAction` | `app/actions/auth.ts` | Вход по email и паролю, запись cookies сессии, редирект на `next` или в кабинет; перевод ошибок |
| `signUpAction` | `app/actions/auth.ts` | Проверка полей (имя, email, пароль ≥ 6), **проверка согласия на обработку данных на сервере**, создание аккаунта, `emailRedirectTo`; при включённом подтверждении — редирект на `/auth/verify-email`, иначе сразу вход |
| `resendVerificationAction` | `app/actions/auth.ts` | Повторная отправка письма подтверждения; ответ нейтральный (без раскрытия существования адреса), отдельное сообщение при лимите отправок |
| `confirmSessionFromTokens` | `app/actions/confirm-session.ts` | Завершает вход по токенам из фрагмента адреса (`#access_token=…`) — вариант для шаблона письма Supabase по умолчанию |
| `completeLesson` | `app/actions/complete-lesson.ts` | Отмечает урок пройденным: `upsert` в `lesson_progress` по (user_id, course_type, lesson_id). Идемпотентно; значения из браузера проверяются (`courseType` только `basic`/`pro`) |
| `completeModuleTest` | `app/actions/complete-module-test.ts` | Принимает результат теста, повторно проверяет порог сдачи, пишет **только сданный** тест (`basic-test` / `pro-test`) |

Типы результатов описаны рядом с функциями (`CompleteLessonResult`, `CompleteModuleTestResult`,
`ConfirmSessionResult`), состояние формы авторизации — `app/actions/auth-state.ts`
(`AuthFormState`: `error`, `info`, введённые значения без пароля).

## 8. Компоненты

| Компонент | Тип | Роль |
|---|---|---|
| `components/Header.tsx` | серверный | Шапка: логотип, навигация, кнопка «Войти» или «Личный кабинет» в зависимости от сессии |
| `components/Footer.tsx` | серверный | Подвал: ссылки на все юридические документы, копирайт, реквизиты владельца и почта |
| `components/CookieBanner.tsx` | клиентский | Баннер согласия на cookie: два варианта, запоминание выбора в `localStorage`, не показывается повторно |
| `components/EmailLinkHandler.tsx` | клиентский | Разбирает `#access_token=…&refresh_token=…` из ссылки письма и завершает вход через `confirmSessionFromTokens`; подключён один раз в корневом layout |
| `components/course/LessonBlocks.tsx` | серверный | Отрисовка всех блоков урока (текст, списки, шаги, врезки, визуализации, мини-проверки) |
| `components/course/CompleteButton.tsx` | клиентский | Кнопка «Пройти урок / Перейти в конструктор»: отмечает урок и переводит дальше; при ошибке сохранения всё равно ведёт вперёд (прогресс не должен блокировать обучение) |
| `components/course/ModuleTest.tsx` | клиентский | Тест модуля: по одному вопросу, пояснение к ответу, финальная оценка и разбор, сохранение результата; два режима — страница (`layout="page"`) и слайд (`layout="overlay"`) |
| `components/course/TestOverlay.tsx` | клиентский | Слайд с тестом поверх урока: затемнение, Escape и клик по фону закрывают, фокус возвращается на кнопку, после сдачи обновляет страницу под слайдом |
| `components/course/ModuleTestGate.tsx` | серверный | Карточка «Проверь себя» на последнем уроке модуля: слайд с тестом и заблокированное продолжение до сдачи |
| `components/course/module-test-theme.ts` | общий (без `"use client"`) | Цвета тестов: синие для базового курса, янтарные для продвинутого |
| `components/course/MiniCheck.tsx` | клиентский | Мини-проверка внутри урока (`mini_check`) — сейчас в данных не используется, компонент рабочий |
| `components/legal/LegalText.tsx` | серверный | Типографика юридических документов: заголовки, списки, врезки, внутренние ссылки |
| `components/lesson-visuals/index.tsx`, `_Wrapper.tsx` | серверный | Реестр визуализаций уроков и общая обёртка (карточка с подписью и рамкой); файлов визуализаций ещё нет |
| `app/subscription/PlanButton.tsx` | клиентский | Кнопка тарифа: пока показывает уведомление «оплата подключается» |
| `app/auth/login/LoginForm.tsx`, `app/auth/sign-up/SignUpForm.tsx`, `app/auth/verify-email/ResendForm.tsx` | клиентские | Формы: `useActionState` + `useFormStatus`-подобное состояние ожидания, показ ошибок и сохранение введённого email после ошибки |

---

## 9. База данных (Supabase)

Self-hosted Supabase на сервере в РФ: используются только Auth и Postgres. Таблиц три, все с
включённым RLS.

| Таблица | Колонки | Назначение |
|---|---|---|
| `profiles` | `id uuid` (= `auth.users.id`), `email`, `name`, `created_at` | Имя и почта для кабинета |
| `lesson_progress` | `id uuid`, `user_id uuid`, `lesson_id integer`, `course_type text not null default 'basic'`, `completed_at` | Прогресс уроков и результаты тестов |
| `subscriptions` | `id uuid`, `user_id uuid`, `status text`, `trial_ends_at`, `created_at`, `updated_at` | Доступ к продвинутому курсу |

Как это работает:

- **Политики «только свои строки»**: `auth.uid() = id` для `profiles`, `auth.uid() = user_id` для
  `lesson_progress` и `subscriptions`. Для `subscriptions` есть **только `select`** — менять статус
  может исключительно `service_role`, то есть подписку нельзя выдать себе из браузера.
- **`anon` доступа не имеет** (PostgREST отвечает `401 permission denied`): таблицы читаются только
  вошедшим пользователем или сервисной ролью. Гранты выданы ролям `authenticated` и `service_role`.
- **Уникальный индекс** `(user_id, course_type, lesson_id)` — на него опирается `upsert` в
  `completeLesson`. Старый ключ `(user_id, lesson_id)` снят: урок №1 есть и в базовом, и в
  продвинутом курсе.
- **Значения `course_type`**: `basic` — уроки базового курса (1…15), `basic-test` — тесты его
  модулей (по `lesson_id` 1…6), `pro` — уроки продвинутого (1…109), `pro-test` — его тесты (по
  `lesson_id` = `module_order`, 1…26).
- **Ограничение `course_type`**: `lesson_progress_course_type_check` допускает все четыре значения —
  `basic`, `pro`, `basic-test`, `pro-test`. В живой базе оно одно время разрешало только `basic` и
  `pro`, из-за чего результаты тестов модулей падали с ошибкой `23514`, а следующий модуль оставался
  закрытым. Корректное определение — `supabase/module_test_course_types.sql`.
- **Статусы подписки**: `active` — доступ всегда; `trial` — пока `trial_ends_at` в будущем;
  остальные (`inactive`, `canceled`, `past_due` и т.п.) — доступа нет.
- **SQL лежит в `supabase/*.sql`** — это справочные копии того, что применено вручную в
  Supabase → SQL Editor: `grants_and_policies.sql` (гранты и RLS), `pro_course_access.sql`
  (колонка `course_type`, уникальный индекс, инструкция как выдать себе trial и как отключить
  доступ) и `module_test_course_types.sql` (ограничение на значения `course_type`).
  Автоматически эти файлы не выполняются.
- **Устойчивость к неполной базе**: если колонки `course_type` ещё нет, кабинет читает прогресс
  без фильтра по курсу и не показывает ноль; если результаты тестов не читаются, модуль не
  запирается; если часть данных не пришла — кабинет показывает предупреждение, а не падает.

## 10. Сборка и деплой

Приложение живёт на **Amvera Cloud** (node 22, `amvera.yml`):

- `build.additionalCommands`: `npm run build:amvera` — `clean` + `ensure:tsconfig` +
  `check:imports` + `npm ci --include=dev --include=optional --no-audit --no-fund` +
  `next build --turbopack`;
- `run`: `node_modules/next/dist/bin/next start`, порт 3000.

Почему сборка устроена именно так (каждый шаг появился из-за реальной поломки на хостинге):

- `ensure:tsconfig` — восстанавливает `tsconfig.json` с алиасом `@/*`, если файл не доехал в
  сборочное дерево;
- `check:imports` — сверяет импорты с именами файлов на диске **с учётом регистра**: Linux регистр
  различает, Windows — нет, поэтому локально всё собиралось, а на Amvera падало «module not found»;
- `.npmrc` → `include=optional` — гарантирует установку `@tailwindcss/oxide-linux-x64-gnu`
  (Tailwind 4 ставит нативный бинарник как optional-зависимость, а сборка идёт с `--omit=optional`);
- `--include=dev` — сборке нужны `typescript` и `tailwindcss`, которые лежат в devDependencies.

Перед деплоем прогоняются: `npm run lint`, `npx tsc --noEmit -p tsconfig.json`,
`npm run check:imports`, `npm run check:docs`, `npm run build`. Проверка сводки в
`build:amvera` намеренно не входит: расхождение в документации не должно останавливать выкладку
работающего сайта. **Автотестов и CI в проекте нет** — см. раздел 12.

Переменные окружения на хостинге: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`,
`SUPABASE_SERVICE_ROLE_KEY` (пока не используется в коде), `NEXT_PUBLIC_SITE_URL`.

---

## 11. Безопасность и защита данных

- **Пароли и сессии — целиком на Supabase Auth**: пароли хранятся хешами (bcrypt), сессия — в
  httpOnly-cookie, приложение никогда не пишет в `auth.users` и не хранит паролей.
- **Клиентского Supabase в браузере нет**: запросы к базе идут только с сервера (Server Components,
  Server Actions), поэтому anon-ключ не используется для чтения данных и токены сессии не попадают
  в бандл.
- **RLS + отсутствие грантов у `anon`**: без входа таблицы недоступны, вошедший видит только свои
  строки. Статус подписки меняет только `service_role` — выдать себе Pro через API нельзя.
- **Заголовок `x-supabase-user`** всегда удаляется и заново подставляется middleware, поэтому
  подделать личность через заголовок нельзя (в нём нет токенов — только id, email, имя).
- **Защита от открытого редиректа**: `resolveAfterAuthPath` пускает только внутренние пути, поэтому
  `?next=//evil.com` никуда не уведёт. В `/auth/confirm` `type` проверяется по белому списку, а
  `next` — той же функцией.
- **Серверная валидация входов**: Server Actions проверяют всё, что приходит из браузера (номер
  урока, тип курса, длина пароля, наличие согласия на обработку данных) — форма считается
  недоверенным источником.
- **Доступ к платному контенту решается на сервере**: страницы продвинутого курса отдают только
  2 блока превью, остальное не попадает в HTTP-ответ.
- **Персональные данные**: собирается минимум (имя, email, прогресс), обработка описана в пяти
  документах, приложение и база развёрнуты на серверах в РФ, данные за пределы РФ не выходят.

## 12. Ограничения и открытые задачи

Что ещё не сделано (по убыванию важности):

1. **Конструктор — только заглушка**: страница `/constructor` больше не отдаёт 404 и объясняет
   статус, но самих шаблонов (вазы, брелоки, светильники) и скачивания моделей пока нет. Вместе
   с ней в тарифах упомянуты и не реализованы AI-диагностика и библиотека моделей — это нужно
   закрыть до старта продаж.
2. **Платежи не подключены**: `PlanButton` показывает уведомление, статус подписки выставляется
   вручную в БД. Оферта и порядок оплаты (ЮKassa, чек НПД через «Мой налог») уже описаны.
3. **Визуализации уроков** (Этап 2): базовый курс — 27 из 27 блоков (M1–M6, уроки 1–15), готов
   целиком; продвинутый раздел — 205 блоков пока рисуются заглушками.
4. **Сброса пароля нет**: если пользователь забыл пароль, восстановиться сам он не может.
5. **Автотестов и CI нет**: проверки — `lint`, `tsc`, `check:imports`, `check:docs` и `build`,
   но запускаются они вручную.
6. **Уведомление Роскомнадзора** о начале обработки персональных данных и публикация политики на
   домене — задача владельца, а не кода; реквизиты в `lib/legal.ts` стоит сверить с актуальными.
7. **Настройки Supabase для писем**: Site URL, Redirect URLs и шаблон `Confirm signup` нужно
   прописать на стороне Supabase, иначе ссылка из письма не приведёт на `/auth/confirm`
   (подробности — README и раздел 3.1).
8. **Неиспользуемый код**: `lib/supabase/client.ts` (браузерный клиент), `components/course/MiniCheck.tsx`
   (блок `mini_check` не встречается в данных) — оставлены как задел, но при ревизии стоит решить
   их судьбу.
9. **`completeLesson` не проверяет подписку**: вошедший пользователь может отметить урок
   продвинутого курса пройденным без подписки. Доступа к контенту это не даёт, но строки в
   `lesson_progress` создаются — если это важно, нужна проверка в action.
10. **Правильные ответы тестов уходят в браузер** (проверка на клиенте, сервер лишь пересчитывает
    число правильных). Для обучающих тестов это приемлемый компромисс, но защитой от накрутки не
    является.
11. **Зависимости**: стоит периодически смотреть `npm audit` и обновлять `next`/`postcss` — на
    момент описания в проекте `next@15.5.25`.

**Закрыто в P0** (было в этом списке раньше): ссылки на конструктор вели в 404; `total_lessons`
в `data/course-pro.json` заявлял 110 уроков при 109; в репозитории лежали неиспользуемые
`data/course.json` и 21 шаблонный SVG (`public/lessons/*.svg` и пять картинок из create-next-app).
Совпадение чисел теперь проверяет `npm run check:docs`.

## 13. История работ

Проект развивался этапами (коммиты в `main`):

| Этап | Что сделано |
|---|---|
| Деплой и сборка | Конфигурация Amvera, linux-бинарник Tailwind, Node 22, проверка импортов (`check:imports`), автовосстановление `tsconfig` — сборка стала воспроизводимой |
| Контент | Полная замена базового курса (6 модулей, 15 уроков) и продвинутого (26 модулей, 109 уроков) на новые данные с блоками, тестами и пояснениями |
| Проверки знаний | Тесты модулей с пояснениями к каждому ответу, обязательность теста перед следующим модулем, перенос проверок из уроков на страницы модулей |
| Юридический контур | Пять документов (152-ФЗ), cookie-баннер, обязательный чекбокс согласия, футер со ссылками, реквизиты в одном файле |
| Цены | Приведение цены Pro к 1 490 ₽ во всех местах (витрина, замок в уроке, оферта) |
| Подтверждение email | Ссылка из письма → сессия: Route Handler `/auth/confirm`, страница статуса с повторной отправкой, клиентский обработчик `#access_token`, гейт неподтверждённой почты в middleware, README с настройкой шаблона письма |
| Сводка по проекту | `PROJECT.md` — единый обзор: функции, блоки, маршруты, данные, база, ограничения; ссылка из README |
| P0-чистка | Конструктор перестал вести в 404 (страница-заглушка + защита пути в middleware), `total_lessons` приведён к фактическим 109, удалены неиспользуемые `data/course.json` и 21 шаблонный SVG, добавлена проверка сводки `check:docs` |
| Тест слайдом | Тест модуля открывается слайдом поверх урока (`TestOverlay` + `ModuleTestGate`): «Продолжить курс» заблокирована до сдачи с подписью «Сначала сдай тест», экран «Модуль закрыт» тоже даёт пройти тест на месте, цвета вынесены в `module-test-theme.ts` |

**Как проверялось (последняя итерация):** `npx tsc --noEmit` — 0 ошибок, `npm run lint` — 0,
`npm run check:imports` — 135 импортов без битых, `npm run check:docs` — сводка сходится,
`npm run build` — `Compiled successfully`. Логика теста проверена в браузере (Chrome + DevTools
Protocol, без установки зависимостей): слайд открывается кликом, прокрутка под ним блокируется,
10 вопросов отвечаются, вердикт и «Продолжить курс» появляются, Escape закрывает слайд и возвращает
фокус, при несданном тесте «Продолжить курс» заблокирована — 21 проверка из 21, плюс скриншоты
в ширине 376px без горизонтального скролла.
Живой прогон прод-сборки локально: 11 сценариев (состояния `/auth/verify-email`, редиректы
`/auth/confirm` с испорченным токеном и недопустимым `type`, регрессии входа, регистрации и
закрытых маршрутов) — все прошли. Доставка реального письма и переход по настоящей ссылке
требуют боевого ящика и настроек Supabase из пункта 7.
